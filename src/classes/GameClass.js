class GameClass {
  constructor(image, title, description, playerNumbers, category) {
    this.image = image;
    this.title = title;
    this.description = description;
    this.playerNumbers = playerNumbers;
    this.category = category;
  }
}

export default GameClass;
