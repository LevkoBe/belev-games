import React from "react";
import "./settingsCommon.css";

const AchievementsSettings = ({ settings, handleSettingsChange }) => {
  return (
    <div className="settings">
      <h2 className="settings">Achievements</h2>
    </div>
  );
};

export default AchievementsSettings;
