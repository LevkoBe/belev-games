import React from "react";

const Room = ({ roomData }) => {
  // Implement a component that represents a customizable room
  // Allow users to set styles (colors, fonts, animations) for the room
  // Allow users to add games to the room as draggable components
};

export default Room;
