import React from "react";

const Navigation = () => {
  return (
    <nav>
      <button>Home</button>
      <button>Games</button>
      <button>About</button>
    </nav>
  );
};

export default Navigation;
